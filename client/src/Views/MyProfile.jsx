import React from "react";
// import { useParams } from "react-router-dom";

const MyProfile = () => {

    // let { userid } = useParams();
    // let [profiles, setProfile] = useState([]);

    // useEffect(() => {
    //     (async function () {
    //         let data = await fetch(`http://localhost:5000/profiles/myProfile/${userid}`).then((results) => results.json());
    //       setProfile(data);
    //         console.log("data", data);
    //     })();
    // }, []);
  return (
    <div>
    <h1>hello</h1>
    {/* // <div>{profiles && profiles.map((profile) => {
    //   const {username} = profile

    //   return (
    //     <h1>{username}</h1>

    //   )
    // })
    // } */}

    
    </div>
  )
}

export default MyProfile