interface Post {
    // single post from API
        id: string;
        title: string;
        description?: string;
        price: number;
        postcode: number;
        category: string;
        postimage: string;
        createdat: string; 
}